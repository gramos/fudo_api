¿Qué es HTTP?
=============

HTTP significa Hypertext Transfer Protocol y es el protocolo que usan clientes y servidores para intercambiar recursos web, como páginas web. Las solicitudes HTTP incluyen un método (GET, por ejemplo), una URL, headers y, opcionalmente, un body. Las respuestas incluyen un código de estado.
